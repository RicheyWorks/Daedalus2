<?xml version="1.0" encoding="UTF-8"?>
<!--
    examples/openxr-plugin — OpenXR headset backend for daedalus-explore.

    Deliberately NOT a reactor child. Headset natives stay off mvn verify
    (ADR-017). Build separately after the reactor install:

        mvn -f examples/openxr-plugin/pom.xml clean package
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.daedalus.examples</groupId>
    <artifactId>daedalus-openxr-plugin</artifactId>
    <version>1.2.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>Daedalus :: Example Plugin :: OpenXR</name>
    <description>
        Reference XrRuntime: present only when DAEDALUS_OPENXR=1. The host
        stays first-person desktop when this JAR is absent.
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <daedalus.version>1.2.0-SNAPSHOT</daedalus.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.daedalus</groupId>
            <artifactId>daedalus-plugin-api</artifactId>
            <version>${daedalus.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.daedalus</groupId>
            <artifactId>daedalus-explore</artifactId>
            <version>${daedalus.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.25.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
        </plugins>
    </build>
</project>
